# Architecture

The project is organized into core annotation models, editing operations, query/indexing utilities, timeline rendering components, GUI modules, I/O, plugins, and AI-assisted services.

Current files are placeholders intended to establish stable module boundaries before implementation begins.
